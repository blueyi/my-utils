# macOS Homebrew package list
# One package per line, # for comment

# core
vim
node
curl
wget
git
zsh
# fish — optional; brew install fish if needed

# c/c++
llvm
cmake
ninja
gdb

# python (uv manages versions + venv + packages; install uv via brew on macOS)
uv

# utils
ctags
