# Lines starting with # are comments
# Fedora/RHEL/CentOS - yum/dnf package names

# editor
vim
neovim

# node (npm included in nodejs on Fedora 24+)
nodejs

# ctags
ctags

# c/c++
gcc
gcc-c++
make
cmake
gdb
ninja-build

# build deps (match running kernel; may fail on minimal install)
kernel-devel-`uname -r`

# python
python3
python3-devel

# pyenv / build deps
openssl-devel
zlib-devel
bzip2-devel
readline-devel
sqlite-devel
ncurses-devel
xz

# llvm (optional, for YCM etc.)
llvm

# utils
curl
wget
openssh-clients
zsh
