{
  "name": "browser_reload",
  "description": "Reload the current page. Useful after making code changes to see updates.",
  "arguments": {
    "type": "object",
    "properties": {
      "viewId": {
        "type": "string",
        "description": "Target browser tab ID. If omitted, uses the last interacted tab."
      },
      "take_screenshot_afterwards": {
        "type": "boolean",
        "description": "When true, takes a screenshot after reload completes. Defaults to false."
      }
    },
    "required": []
  }
}