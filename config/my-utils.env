# Use MY_UTILS_ROOT if set (e.g. in ~/.my-utils.env), else default by $HOME (works on macOS/Linux/Windows)
export MY_UTILS_ROOT="${MY_UTILS_ROOT:-$HOME/workspace/my-utils}"
export MYRC_PATH="${MY_UTILS_ROOT}/config"
