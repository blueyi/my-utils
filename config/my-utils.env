export MY_UTILS_ROOT="$HOME/workspace/my-utils"
export MYRC_PATH="$MY_UTILS_ROOT/config"

# Git dual-remote (GitHub + GitCode) — see config/git-dual-remote.env for defaults.
# export GIT_DUAL_REMOTE_ENABLED=1
# export GIT_DUAL_REMOTE_GITHUB_USER=blueyi
# export GIT_DUAL_REMOTE_GITCODE_USER=blueyi
# export GIT_DUAL_REMOTE_TIMEOUT=20
